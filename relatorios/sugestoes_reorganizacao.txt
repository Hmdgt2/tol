🧠 Sugestões de Reorganização Funcional:

🔧 Função `euler_totient` aparece em múltiplos módulos: matematica_especial, teoria_numeros
   ➤ Sugestão: manter em `matematica_especial.py`, remover duplicados

🔧 Função `gamma_func` aparece em múltiplos módulos: matematica_especial, funcoes_especiais
   ➤ Sugestão: manter em `matematica_especial.py`, remover duplicados

🔧 Função `beta_func` aparece em múltiplos módulos: matematica_especial, funcoes_especiais
   ➤ Sugestão: manter em `matematica_especial.py`, remover duplicados

🔧 Função `bessel_j` aparece em múltiplos módulos: matematica_especial, funcoes_especiais
   ➤ Sugestão: manter em `matematica_especial.py`, remover duplicados

🔧 Função `catalan_number` aparece em múltiplos módulos: numeros_especiais, combinatoria_avancada_1
   ➤ Sugestão: manter em `numeros_especiais.py`, remover duplicados

🔧 Função `bell_number` aparece em múltiplos módulos: numeros_especiais, teoria_numeros
   ➤ Sugestão: manter em `numeros_especiais.py`, remover duplicados

🔧 Função `partition_number` aparece em múltiplos módulos: numeros_especiais, teoria_numeros
   ➤ Sugestão: manter em `numeros_especiais.py`, remover duplicados

🔧 Função `bernoulli_number` aparece em múltiplos módulos: numeros_especiais, teoria_numeros
   ➤ Sugestão: manter em `numeros_especiais.py`, remover duplicados

🔧 Função `mutual_info` aparece em múltiplos módulos: estatistica_multivariada, teoria_informacao
   ➤ Sugestão: manter em `estatistica_multivariada.py`, remover duplicados

🔧 Função `normalized_mutual_info` aparece em múltiplos módulos: estatistica_multivariada, teoria_informacao
   ➤ Sugestão: manter em `estatistica_multivariada.py`, remover duplicados

🔧 Função `fft_phase` aparece em múltiplos módulos: temporais, processamento_sinal
   ➤ Sugestão: manter em `temporais.py`, remover duplicados

🔧 Função `ifft_real` aparece em múltiplos módulos: temporais, processamento_sinal
   ➤ Sugestão: manter em `temporais.py`, remover duplicados

🔧 Função `unique_count` aparece em múltiplos módulos: estatisticas, conjuntos
   ➤ Sugestão: manter em `estatisticas.py`, remover duplicados

🔧 Função `intersection` aparece em múltiplos módulos: estatisticas, conjuntos
   ➤ Sugestão: manter em `estatisticas.py`, remover duplicados

🔧 Função `union` aparece em múltiplos módulos: estatisticas, conjuntos
   ➤ Sugestão: manter em `estatisticas.py`, remover duplicados

🔧 Função `mirror_count` aparece em múltiplos módulos: estatisticas, conjuntos
   ➤ Sugestão: manter em `estatisticas.py`, remover duplicados

🔧 Função `pair_sum_count` aparece em múltiplos módulos: estatisticas, conjuntos
   ➤ Sugestão: manter em `estatisticas.py`, remover duplicados

🔁 Funções equivalentes: bessel_j
   ➤ Sugestão: fundir em `matematica_especial.py` com nome padronizado

🔁 Funções equivalentes: unique_count
   ➤ Sugestão: fundir em `estatisticas.py` com nome padronizado

🔁 Funções equivalentes: intersection
   ➤ Sugestão: fundir em `estatisticas.py` com nome padronizado

🔁 Funções equivalentes: union
   ➤ Sugestão: fundir em `estatisticas.py` com nome padronizado

🔁 Funções equivalentes: mirror_count
   ➤ Sugestão: fundir em `estatisticas.py` com nome padronizado

🔁 Funções equivalentes: pair_sum_count
   ➤ Sugestão: fundir em `estatisticas.py` com nome padronizado

🔍 Funções semelhantes `shortest_path_all_pairs` (algoritmos_grafos) ≈ `shortest_paths_length` (grafos)
   🔁 Similaridade: 92.52%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `gamma_func` (matematica_especial) ≈ `gamma_func` (funcoes_especiais)
   🔁 Similaridade: 95.24%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `digamma_func` (matematica_especial) ≈ `gamma_func` (funcoes_especiais)
   🔁 Similaridade: 88.64%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `beta_func` (matematica_especial) ≈ `beta_func` (funcoes_especiais)
   🔁 Similaridade: 95.0%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `bessel_y` (matematica_especial) ≈ `bessel_j` (funcoes_especiais)
   🔁 Similaridade: 88.89%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `chebyshev_t` (matematica_especial) ≈ `chebyshev_T` (funcoes_especiais)
   🔁 Similaridade: 86.49%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `fibonacci` (numeros_especiais) ≈ `fibonacci_num` (teoria_numeros)
   🔁 Similaridade: 96.0%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `lucas` (numeros_especiais) ≈ `lucas_num` (teoria_numeros)
   🔁 Similaridade: 95.41%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `catalan_number` (numeros_especiais) ≈ `catalan_num` (teoria_numeros)
   🔁 Similaridade: 95.73%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `bell_number` (numeros_especiais) ≈ `bell_number` (teoria_numeros)
   🔁 Similaridade: 95.24%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `bell_number` (numeros_especiais) ≈ `bernoulli_number` (teoria_numeros)
   🔁 Similaridade: 86.96%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `partition_number` (numeros_especiais) ≈ `partition_number` (teoria_numeros)
   🔁 Similaridade: 95.73%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `bernoulli_number` (numeros_especiais) ≈ `bell_number` (teoria_numeros)
   🔁 Similaridade: 86.96%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `bernoulli_number` (numeros_especiais) ≈ `bernoulli_number` (teoria_numeros)
   🔁 Similaridade: 96.0%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `sum_combinations2` (combinatoria_avancada) ≈ `sum_of_pairs` (analise_padroes)
   🔁 Similaridade: 87.43%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `sum_combinations2` (combinatoria_avancada) ≈ `sum_of_triples` (analise_padroes)
   🔁 Similaridade: 87.43%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `sum_combinations3` (combinatoria_avancada) ≈ `sum_of_pairs` (analise_padroes)
   🔁 Similaridade: 86.23%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `sum_combinations3` (combinatoria_avancada) ≈ `sum_of_triples` (analise_padroes)
   🔁 Similaridade: 88.62%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `intersection` (estatisticas) ≈ `union` (conjuntos)
   🔁 Similaridade: 85.53%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `union` (estatisticas) ≈ `intersection` (conjuntos)
   🔁 Similaridade: 85.53%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

🔍 Funções semelhantes `pair_product_count` (estatisticas) ≈ `pair_sum_count` (conjuntos)
   🔁 Similaridade: 92.99%
   ➤ Sugestão: avaliar fusão ou renomeação para consistência

